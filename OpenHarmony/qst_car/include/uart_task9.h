#ifndef UART_TASK9_H
#define UART_TASK9_H

/**
 * @brief Creates the UART2 receive and message-processing tasks for task 9.
 *
 * Peripheral_Init() must be called before this function so UART2 is ready.
 */
void UartTask9Init(void);

#endif
